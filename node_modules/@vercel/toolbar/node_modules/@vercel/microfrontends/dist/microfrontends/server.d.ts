import { C as ConfigMeta } from '../index-2b59c627.js';
import { C as Config } from '../index-2f78c0ca.js';
import { O as OverridesConfig } from '../types-b6d38aea.js';
import { Microfrontends } from '../microfrontends.js';
import '../types-4ef2bddb.js';

declare class MicrofrontendsServer extends Microfrontends {
    /**
     * Writes the configuration to a file.
     */
    writeConfig(opts?: {
        pretty?: boolean;
    }): void;
    /**
     * Generates a MicrofrontendsServer instance from an unknown object.
     */
    static fromUnknown({ config, cookies, meta, }: {
        config: unknown;
        cookies?: {
            name: string;
            value: string;
        }[];
        meta: ConfigMeta;
    }): MicrofrontendsServer;
    /**
     * Generates a MicrofrontendsServer instance from the environment.
     * Uses additional validation that is only available when in a node runtime
     */
    static fromEnv({ cookies, meta, }: {
        cookies: {
            name: string;
            value: string;
        }[];
        meta: ConfigMeta;
    }): MicrofrontendsServer;
    /**
     * Validates the configuration against the JSON schema
     */
    static validate(config: string | Config): Config;
    /**
     * Looks up the configuration by inferring the package root and looking for a microfrontends config file. If a file is not found,
     * it will look for a package in the repository with a microfrontends file that contains the current application
     * and use that configuration.
     *
     * This can return either a Child or Main configuration.
     */
    static infer({ directory, filePath, meta, cookies, options, }?: {
        directory?: string;
        filePath?: string;
        meta?: ConfigMeta;
        cookies?: {
            name: string;
            value: string;
        }[];
        options?: {
            resolveMainConfig?: boolean;
        };
    }): MicrofrontendsServer;
    static fromFile({ filePath, cookies, meta, options, }: {
        filePath: string;
        meta: ConfigMeta;
        cookies?: {
            name: string;
            value: string;
        }[];
        options?: {
            resolveMainConfig?: boolean;
        };
    }): MicrofrontendsServer;
    static fromMainConfigFile({ filePath, overrides, }: {
        filePath: string;
        overrides?: OverridesConfig;
    }): MicrofrontendsServer;
}

export { MicrofrontendsServer };
